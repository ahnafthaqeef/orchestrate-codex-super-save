# Orchestrate Codex Super Save

Use `.agents/skills/orchestrate-super-save/SKILL.md` only when the user invokes
`$orchestrate-super-save` or explicitly asks for the Super Save routing mode.
Keep the main agent responsible for planning, integration, and final verification.
Do not overwrite or weaken an existing Orchestrate installation.

Validate changes with `python scripts/validate.py` and
`python scripts/test_install.py`. Keep credentials, private memories, and personal
data out of this portable bundle. Before the first push, verify repository visibility.

